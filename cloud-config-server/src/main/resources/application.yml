server:
  port: 9296

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          # Config in git online
          uri: https://github.com/trungtrong2010/config-server-microservice
          clone-on-start: true